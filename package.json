{
  "name": "@forkless/core",
  "version": "0.1.0",
  "description": "Config-driven journey state machine with conversational interface",
  "main": "index.js",
  "scripts": {
    "test": "node --test test/**/*.test.js",
    "test:verbose": "node --test --test-reporter spec test/**/*.test.js"
  },
  "keywords": ["state-machine", "journey", "conversational"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  }
}
